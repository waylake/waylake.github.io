# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.
#
# If you need help with YAML syntax, here are some quick references for you:
# https://learn-the-web.algonquindesign.ca/topics/markdown-yaml-cheat-sheet/#yaml
# https://learnxinyminutes.com/docs/yaml/
#
# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.

title: Waylake's Thinking
description: GitHub 협업 전략 및 개발 관련 생각 정리
description_ko: GitHub 협업 문제 해결 및 개발 워크플로우 최적화에 대한 생각과 전략을 공유합니다.
baseurl: ""
url: "https://waylake.github.io"
twitter_username: waylake
github_username: waylake

# Build settings
theme: minima
plugins:
  - jekyll-feed
  - jekyll-seo-tag
  - jekyll-sitemap

# Exclude from processing.
exclude:
  - .sass-cache/
  - .jekyll-cache/
  - gemfiles/
  - Gemfile
  - Gemfile.lock
  - node_modules/
  - vendor/bundle/
  - vendor/cache/
  - vendor/gems/
  - vendor/ruby/

# Korean language support
lang: ko
encoding: utf-8
timezone: Asia/Seoul

# Collections
collections:
  areas:
    output: true
    permalink: /areas/:path/

# Defaults
defaults:
  - scope:
      path: ""
      type: "posts"
    values:
      layout: "post"
      author: "waylake"
  - scope:
      path: ""
      type: "areas"
    values:
      layout: "page"
      author: "waylake"

# Minima theme settings
minima:
  skin: dark
  date_format: "%Y년 %m월 %d일"
  social_links:
    - { platform: github, user_url: "https://github.com/waylake" }
    - { platform: twitter, user_url: "https://twitter.com/waylake" }
